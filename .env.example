# Site URL - Used for sitemap, metadata, and Open Graph tags
# REQUIRED: This environment variable must be set, otherwise the build will fail
NEXT_PUBLIC_SITE_URL=http://localhost:8000

# Google Analytics ID - Used for analytics tracking
# Optional: If not set, Google Analytics will not be loaded
NEXT_PUBLIC_GA_ID=G-SBRC1GSMZS

